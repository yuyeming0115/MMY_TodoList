{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-scope",
  "description": "Allow shell commands for screenshot",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": []
    }
  ]
}
