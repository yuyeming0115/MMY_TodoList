<!-- MMY-INJECT:START -->
## 模型信息（由 MMYCodeSwitch-API 自动管理）

> 当前使用的模型为 **glm-5**，通过 **阿里云百炼-glm5** 提供服务。
> 此段内容由 `<!-- MMY-INJECT:START -->` 和 `<!-- MMY-INJECT:END -->` 包裹，
> 切换供应商或解绑项目时会自动更新，请勿手动编辑此区域。

<!-- MMY-INJECT:END -->

---

## 开发流程规范

### 需求分级与 PR 判断

每次接收到新需求时，按以下标准判断是否需要开 PR：

| 需求类型 | 是否开PR | 示例 |
|---------|---------|------|
| **新功能开发** | ✅ 必须 | 添加拖拽定位、新增按钮类型 |
| **架构重构** | ✅ 必须 | 模块拆分、API重构 |
| **UI/交互优化** | ✅ 必须 | 交互逻辑改变、新增设置项 |
| **Bug修复** | ❌ 可直接提交 | 修复崩溃、显示异常、逻辑错误 |
| **文档更新** | ❌ 可直接提交 | README、开发文档维护 |
| **代码微调** | ❌ 可直接提交 | 变量命名、注释补充、格式整理 |

### 开发流程

1. **需求分析** → 判断是否需要开 PR
2. **若需开 PR**：
   - 创建功能分支（命名如 `feature/hud-drag-position`）
   - 创建需求方案文档（存放于 `开发文档/` 目录）
   - 开发、测试、调试
   - 完成后创建 PR，等待用户确认合并
3. **跟进合并** — PR 创建后主动询问用户是否合并入 main
4. **若不需开 PR** → 直接在 main 分支完成并提交

### 合并确认机制

- PR 创建完成后，必须询问用户：「PR 已创建，是否合并入 main 分支？」
- 用户确认后才执行合并操作
- 合并后及时更新相关开发文档

---

## 模块化开发文档管理

每次开发新功能或新需求时，按以下流程处理：

1. **分析模块归属** — 判断该功能属于哪个现有模块
2. **文档操作**：
   - 有对应模块 → 更新该模块的开发文档
   - 无对应模块 → 新建开发文档（命名清晰，如 `HUD交互模块.md`）
3. **持续维护** — 后续围绕该模块的任何优化、调整、Bug修复，都要同步更新对应的开发文档

**目的**：确保开发文档与代码实现保持同步，方便追溯和查阅。

---

## 发布打包流程

### 触发条件

当以下情况发生并**用户确认功能正常**后，询问用户是否需要打包发布：

1. **新功能完成** — 功能开发完成，用户测试确认可用
2. **Bug修复完成** — Bug修复完成，用户验证问题已解决
3. **重要更新完成** — 性能优化、UI改进等，用户确认效果满意

### 打包流程

1. **询问用户**：「功能已完成确认正常，是否需要打包发布？」
2. **用户确认** → 执行 `./release.sh` 打包脚本
3. **输出目录**：`releases/v{版本号}_{时间戳}/`
   - 例如：`releases/v1.11.0_20260521_143000/`
4. **生成文件**：
   - **Windows**：安装包 `.exe`（包含便携版）
   - **macOS**：DMG 安装包 `.dmg`
5. **发布说明**：自动生成 `README.txt`，包含版本号、时间戳、文件列表

### 版本号管理

版本号位于两处，打包时需确保一致：
- `desktop/package.json` → `"version": "1.11.0"`
- `desktop/src-tauri/Cargo.toml` → `version = "1.11.0"`

### 发布后操作

打包完成后，提示用户：
- 发布目录位置
- 可用于分发的文件列表
- 可直接发送给他人测试