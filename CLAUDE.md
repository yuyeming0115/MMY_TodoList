
<!-- MMY-INJECT:START -->
## 模型信息（由 MMYCodeSwitch-API 自动管理）

> 当前使用的模型为 **glm-5**，通过 **阿里云百炼-glm5** 提供服务。
> 此段内容由 `<!-- MMY-INJECT:START -->` 和 `<!-- MMY-INJECT:END -->` 包裹，
> 切换供应商或解绑项目时会自动更新，请勿手动编辑此区域。

<!-- MMY-INJECT:END -->---

## 开发流程规范

### 需求分级与 PR 判断

每次接收到新需求时，按以下标准判断是否需要开 PR：

| 需求类型 | 是否开PR | 示例 |
|---------|---------|------|
| **新功能开发** | ✅ 必须 | 添加拖拽定位、新增按钮类型 |
| **架构重构** | ✅ 必须 | 模块拆分、API重构 |
| **UI/交互优化** | ✅ 必须 | 交互逻辑改变、新增设置项 |
| **Bug修复** | ❌ 可直接提交 | 修复崩溃、显示异常、逻辑错误 |
| **文档更新** | ❌ 可直接提交 | README、开发文档维护 |
| **代码微调** | ❌ 可直接提交 | 变量命名、注释补充、格式整理 |

### 开发流程

1. **需求分析** → 判断是否需要开 PR
2. **若需开 PR**：
   - 创建功能分支（命名如 `feature/hud-drag-position`）
   - 创建需求方案文档（存放于 `开发文档/` 目录）
   - 开发、测试、调试
   - 完成后创建 PR，等待用户确认合并
3. **跟进合并** — PR 创建后主动询问用户是否合并入 main
4. **若不需开 PR** → 直接在 main 分支完成并提交

### 合并确认机制

- PR 创建完成后，必须询问用户：「PR 已创建，是否合并入 main 分支？」
- 用户确认后才执行合并操作
- 合并后及时更新相关开发文档

---

## 模块化开发文档管理

每次开发新功能或新需求时，按以下流程处理：

1. **分析模块归属** — 判断该功能属于哪个现有模块
2. **文档操作**：
   - 有对应模块 → 更新该模块的开发文档
   - 无对应模块 → 新建开发文档（命名清晰，如 `HUD交互模块.md`）
3. **持续维护** — 后续围绕该模块的任何优化、调整、Bug修复，都要同步更新对应的开发文档

**目的**：确保开发文档与代码实现保持同步，方便追溯和查阅。

---

## 发布打包流程

### 自动触发规则

当用户回复以下关键词时，**自动询问是否需要打包发布**：
- `ok`、`好的`、`可以`、`没问题`
- `解决了`、`修好了`、`正常了`
- `完成`、`测试通过`、`效果不错`

### 打包流程

用户确认打包 → 执行以下步骤：

1. **确认版本号一致**（两处）：
   - `desktop/package.json` → `"version"`
   - `desktop/src-tauri/Cargo.toml` → `version =`
   如不一致，询问用户是否需要更新版本号

2. **执行打包命令**：
   - Windows：`npm run tauri build -- --bundles nsis`（在 `desktop` 目录）
   - macOS：`npm run tauri build -- --bundles dmg`

3. **复制到发布目录**：
   - 创建 `releases/v{版本号}_{时间戳}/`
   - 复制安装包文件（`.exe` 或 `.dmg`）

4. **生成 README.txt**（包含版本号、时间戳、文件列表）

### 发布后提示

告知用户：
- 发布目录位置
- 可用于分发的文件列表

---

## Claude 操作授权

当用户说「我授权给你」或类似授权语句时，Claude 可以自动执行以下操作，无需再次确认：

### 授权操作范围

| 操作类型 | 命令示例 |
|---------|---------|
| **Git 推送** | `git push`、`git push --force-with-lease` |
| **PR 合并** | `gh pr merge` |
| **分支切换** | `git checkout`、`git pull` |
| **打包发布** | `npm run tauri build` |
| **提交代码** | `git commit`、`git add` |

### 授权后自动流程

用户授权后，Claude 自动执行：

1. **推送代码** → 推送当前分支到远程
2. **合并 PR** → 合并已创建的 PR 到 main
3. **切换分支** → 切换到 main 并拉取最新代码
4. **打包发布** → 执行打包流程（见上方「发布打包流程」）

### 注意事项

- 授权仅对当前会话有效，新会话需要重新授权
- 每次授权后 Claude 会主动汇报执行进度
- 如遇错误，Claude 会暂停并报告问题，等待用户决定